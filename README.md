# Vi-Notes

A full-stack writing verification system that tracks user typing behavior using keystrokes and paste detection. Built with React, Node.js, and JWT authentication, it stores sessions and analyzes content authenticity to distinguish between manually typed and copied text.

## Run Locally

1. Install dependencies
   `npm install`

2. Start the app
   `npm run dev`
